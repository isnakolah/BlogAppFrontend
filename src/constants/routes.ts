class routes {
  home = "/";

  newPosts = {
    all: "/new-posts",
  };

  readPosts = {
    all: "/read-posts",
  };

  likedPosts = {
    all: "/liked-posts",
  };
}

export default routes;
